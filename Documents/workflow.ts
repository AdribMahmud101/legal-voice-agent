
import { DEEPINFRA_BASE_URL } from "@/lib/chat/deepinfra"
import { ObjectPair } from "zod/v3"

const CREATED_AT = time.now();

async function fetchusername(userid: string): Promise<string?> {
  return DEEPINFRA_BASE_URL;
}


const fetchUser = async( userId: string): promise<number> => {
  return CREATED_AT;
}
